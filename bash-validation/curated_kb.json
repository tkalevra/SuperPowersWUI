{
  "ssh": {
    "invalid_subcommands": ["put", "get", "mput", "mget"],
    "note": "ssh executes shell commands, not file transfer operations"
  },
  "sftp": {
    "valid_subcommands": ["put", "get", "cd", "lcd", "ls", "pwd", "bye", "quit"],
    "note": "sftp batch mode: sftp -b - user@host"
  },
  "scp": {
    "invalid_subcommands": ["put", "get", "bye", "quit", "cd"],
    "note": "scp uses direct file paths, not interactive subcommands"
  },
  "rsync": {
    "valid_flags": ["-a", "-v", "-z", "-r", "-e", "-n", "--dry-run", "--checksum", "--partial", "--delete", "--progress"],
    "note": "Use -e for SSH transport options: rsync -e 'ssh -o StrictHostKeyChecking=yes -i keyfile' src/ user@host:/dst/"
  }
}
